config {
  type: "table",
  schema: "raw_vault",
  tags: ["satellite"]
}

SELECT
  MD5(BookingID) AS HK_BookingID,
  ,
  CURRENT_TIMESTAMP() AS LOAD_DTS,
  '' AS REC_SRC
FROM ${ref("")}
WHERE BookingID IS NOT NULL
GROUP BY BookingID