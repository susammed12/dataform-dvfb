config {
  type: "table",
  schema: "raw_vault",
  tags: ["satellite"]
}

SELECT
  MD5(PassengerID) AS HK_PassengerID,
  MD5(CONCAT()) AS HASHDIFF,
  ,
  CURRENT_TIMESTAMP() AS LOAD_DTS,
  'undefined' AS REC_SRC
FROM ${ref("undefined")}
WHERE PassengerID IS NOT NULL