config {
  type: "table",
  schema: "raw_vault",
  tags: ["satellite"]
}

SELECT
  MD5(FlightID) AS HK_FlightID,
  ,
  CURRENT_TIMESTAMP() AS LOAD_DTS,
  '' AS REC_SRC
FROM ${ref("")}
WHERE FlightID IS NOT NULL
GROUP BY FlightID